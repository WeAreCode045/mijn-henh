
/**
 * Enum for property tab values
 */
export enum PropertyTabsValue {
  DASHBOARD = 'dashboard',
  CONTENT = 'content',
  MEDIA = 'media',
  COMMUNICATIONS = 'communications'
}

/**
 * Type for different view states in the application
 */
export type ViewState = 'list' | 'create' | 'edit' | 'detail' | 'settings';
