import { PropertyFormData } from "@/types/property";

interface LocationStepProps {
  // No props required for now
}

export function LocationStep() {
  return (
    <div>
      {/* Implement location step UI */}
      <p>Location Step</p>
    </div>
  );
}
